<?php
/**
 * Copyright © Vendor. All rights reserved.
 * @var \Vendor\ProductQnA\Block\Question\Form $block
 */
$product = $block->getProduct();
if (!$product) {
    return;
}
?>

<div class="ask-question-form-container">
    <div class="page-title-wrapper">
        <h1 class="page-title">
            <span><?= $block->escapeHtml(__('Ask a Question')) ?></span>
        </h1>
    </div>

    <div class="product-info-summary">
        <div class="product-name">
            <?= $block->escapeHtml(__('About: %1', $product->getName())) ?>
        </div>
    </div>

    <form class="form question-form" 
          action="<?= $block->escapeUrl($block->getSaveUrl()) ?>" 
          method="post" 
          id="question-form">
        <?= $block->getBlockHtml('formkey') ?>
        
        <input type="hidden" name="product_id" value="<?= (int)$product->getId() ?>" />
        
        <fieldset class="fieldset">
            <legend class="legend"><span><?= $block->escapeHtml(__('Question Details')) ?></span></legend>
            
            <div class="field question required">
                <label class="label" for="question_text">
                    <span><?= $block->escapeHtml(__('Your Question')) ?></span>
                </label>
                <div class="control">
                    <textarea name="question_text" 
                              id="question_text" 
                              class="input-text" 
                              rows="5" 
                              required
                              placeholder="<?= $block->escapeHtml(__('Type your question here...')) ?>"></textarea>
                </div>
            </div>

            <div class="field answer-preference required">
                <label class="label">
                    <span><?= $block->escapeHtml(__('Who should answer your question?')) ?></span>
                </label>
                <div class="control answer-preference-options">
                    <div class="preference-option">
                        <input type="radio" 
                               name="answer_preference" 
                               id="preference_ai" 
                               value="ai" 
                               checked />
                        <label for="preference_ai" class="preference-label">
                            <span class="preference-icon ai-icon">🤖</span>
                            <span class="preference-content">
                                <strong><?= $block->escapeHtml(__('AI Assistant')) ?></strong>
                                <small><?= $block->escapeHtml(__('Get an instant AI-generated answer')) ?></small>
                            </span>
                        </label>
                    </div>
                    <div class="preference-option">
                        <input type="radio" 
                               name="answer_preference" 
                               id="preference_admin" 
                               value="admin" />
                        <label for="preference_admin" class="preference-label">
                            <span class="preference-icon admin-icon">👨‍💼</span>
                            <span class="preference-content">
                                <strong><?= $block->escapeHtml(__('Store Admin')) ?></strong>
                                <small><?= $block->escapeHtml(__('Get a manually reviewed answer from our team')) ?></small>
                            </span>
                        </label>
                    </div>
                </div>
            </div>

            <div class="field name required">
                <label class="label" for="customer_name">
                    <span><?= $block->escapeHtml(__('Your Name')) ?></span>
                </label>
                <div class="control">
                    <input type="text" 
                           name="customer_name" 
                           id="customer_name" 
                           class="input-text" 
                           value="<?= $block->escapeHtml($block->getCustomerName()) ?>"
                           <?= $block->isLoggedIn() ? 'readonly' : '' ?>
                           required />
                </div>
            </div>

            <div class="field email required">
                <label class="label" for="customer_email">
                    <span><?= $block->escapeHtml(__('Your Email')) ?></span>
                </label>
                <div class="control">
                    <input type="email" 
                           name="customer_email" 
                           id="customer_email" 
                           class="input-text" 
                           value="<?= $block->escapeHtml($block->getCustomerEmail()) ?>"
                           <?= $block->isLoggedIn() ? 'readonly' : '' ?>
                           required />
                </div>
            </div>
        </fieldset>

        <div class="actions-toolbar">
            <div class="primary">
                <button type="submit" class="action submit primary">
                    <span><?= $block->escapeHtml(__('Submit Question')) ?></span>
                </button>
            </div>
            <div class="secondary">
                <a class="action back" href="<?= $block->escapeUrl($product->getProductUrl()) ?>">
                    <span><?= $block->escapeHtml(__('Back to Product')) ?></span>
                </a>
            </div>
        </div>
    </form>
</div>

<style>
.ask-question-form-container {
    max-width: 800px;
    margin: 0 auto;
    padding: 20px;
}

.product-info-summary {
    background: #f5f5f5;
    padding: 15px;
    margin-bottom: 30px;
    border-radius: 4px;
}

.product-name {
    font-size: 16px;
    font-weight: 500;
    color: #333;
}

.question-form .fieldset {
    margin-bottom: 30px;
}

.question-form .field {
    margin-bottom: 20px;
}

.question-form .label {
    font-weight: 600;
    margin-bottom: 8px;
    display: block;
}

.question-form .input-text,
.question-form textarea {
    width: 100%;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 4px;
    font-size: 14px;
}

.question-form textarea {
    resize: vertical;
    min-height: 120px;
}

.question-form .actions-toolbar {
    display: flex;
    gap: 15px;
    margin-top: 20px;
}

.question-form .action.submit {
    background: #1979c3;
    color: #fff;
    padding: 12px 30px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 16px;
    font-weight: 500;
}

.question-form .action.submit:hover {
    background: #006bb4;
}

.question-form .action.back {
    color: #1979c3;
    text-decoration: none;
    padding: 12px 20px;
    display: inline-block;
}

.question-form .action.back:hover {
    text-decoration: underline;
}

/* Answer Preference Radio Buttons */
.answer-preference-options {
    display: flex;
    gap: 15px;
    flex-wrap: wrap;
}

.preference-option {
    flex: 1;
    min-width: 250px;
}

.preference-option input[type="radio"] {
    display: none;
}

.preference-label {
    display: flex;
    align-items: center;
    padding: 15px;
    border: 2px solid #ddd;
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.3s ease;
    background: #fff;
}

.preference-label:hover {
    border-color: #1979c3;
    background: #f9f9f9;
}

.preference-option input[type="radio"]:checked + .preference-label {
    border-color: #1979c3;
    background: #e8f4fd;
    box-shadow: 0 0 0 1px #1979c3;
}

.preference-icon {
    font-size: 32px;
    margin-right: 12px;
    flex-shrink: 0;
}

.preference-content {
    display: flex;
    flex-direction: column;
}

.preference-content strong {
    font-size: 16px;
    color: #333;
    margin-bottom: 4px;
}

.preference-content small {
    font-size: 13px;
    color: #666;
    line-height: 1.4;
}

.preference-option input[type="radio"]:checked + .preference-label .preference-content strong {
    color: #1979c3;
}

/* AI Icon specific styling */
.ai-icon {
    filter: grayscale(50%);
}

.preference-option input[type="radio"]:checked + .preference-label .ai-icon {
    filter: grayscale(0%);
    animation: pulse 0.5s ease;
}

@keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.1); }
}

/* Responsive design */
@media (max-width: 768px) {
    .answer-preference-options {
        flex-direction: column;
    }
    
    .preference-option {
        min-width: 100%;
    }
}
</style>
